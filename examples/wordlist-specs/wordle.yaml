# Wordle-style 5-letter words

character:
  exact_length: 5
  pattern: "^[a-z]+$"

phrase:
  max_words: 1

frequency:
  min_tier: A
  max_tier: N   # top ~30k words

exclude:
  register: [vulgar, offensive, derogatory]
